1, 信号量， Semaphore 一种计数器， 多个共享资源访问 （大多数语言都提供这种机制）

2，CountDownLatch  java提供， 实现是在完成一组正在其他线程中执行的操作之前，允许线程一直等待，

3，CyclicBarrier : java 同步辅助类，  允许多个线程在某个集合点处进行互相等待


4，Phaser j7  把并发任务分成多个阶段， 在开始下个阶段前，当前阶段所以线程必须执行完成

5，Exchanger 提供两个线程之前的数据交换点

